# This file is not executed. It declares development and dashboard tooling so
# renv can keep these packages in the project lockfile intentionally.
if (FALSE) {
  library(bslib)
  library(knitr)
  library(rmarkdown)
  library(testthat)
}

